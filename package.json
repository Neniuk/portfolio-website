{
    "name": "portfolio-website-refactor",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "engines": {
        "node": "22.3.0",
        "npm": "10.8.1"
    },
    "scripts": {
        "start": "cd server && npm start",
        "test": "echo \"Error: no test specified\" && exit 1",
        "heroku-postbuild": "cd client && npm install && npm run build:to:server && cd ../server && npm install && npm run build",
        "prepare": "husky"
    },
    "lint-staged": {
        "*.{js,jsx,ts,tsx}": "eslint --fix",
        "*": "prettier --ignore-unknown --write --concurrent false"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "typescript": "^5.4.5"
    },
    "devDependencies": {
        "husky": "^9.1.4",
        "lint-staged": "^15.2.7"
    }
}
