{
    "name": "portfolio-website-refactor",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "start": "cd server && npm start",
        "test": "echo \"Error: no test specified\" && exit 1",
        "heroku-postbuild": "cd client && npm install && npm run build:to:server && cd ../server && npm install && npm run build"
    },
    "author": "",
    "license": "ISC",
	"devDependencies": {
		"typescript": "^5.3.3"
	}
}
